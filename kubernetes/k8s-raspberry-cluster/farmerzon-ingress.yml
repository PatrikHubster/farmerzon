apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: http-ingress
spec:
  rules:
  - http:
      paths:
      - path: /address
        backend:
          serviceName: farmerzon-address-np
          servicePort: 5000